# =============================================================================
# requirements.txt
# Thesis: A Neuro-Symbolic Decision Layer over Time-Series Foundation Model
#         (MOMENT) Embeddings — Automotive CAN Bus Anomaly Detection
#
# Pinned from the working environment (2026-07-01):
#   Windows 11 · Python 3.14.0 · NVIDIA RTX 5050 Laptop GPU (Blackwell, sm_120, 8 GB)
#
# -----------------------------------------------------------------------------
# TWO REPRODUCTION GOTCHAS (see memory/dev-environment):
#
#   1) PyTorch must be the CUDA 12.8 (cu128) build for Blackwell sm_120.
#      The --extra-index-url below lets pip fetch torch==2.11.0+cu128.
#      A plain `pip install torch` gets a CPU-only wheel that will NOT run
#      on the RTX 5050.
#
#   2) momentfm 0.1.4 hard-pins numpy==1.25.2 / transformers==4.33.3 /
#      huggingface-hub==0.24.0 — none build/resolve on Python 3.14. It must
#      be installed WITHOUT its dependencies, AFTER everything else.
# -----------------------------------------------------------------------------
# INSTALL (two steps):
#     pip install -r requirements.txt
#     pip install --no-deps momentfm==0.1.4
# -----------------------------------------------------------------------------
# Scope: thesis stack only. The global env also holds unrelated packages from
# other work (gymnasium, mujoco, stable_baselines3, opencv-python, tensorboard,
# pygame-ce, ...) — intentionally excluded. See requirements.lock.txt for the
# complete `pip freeze` snapshot.
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cu128

# --- Deep-learning backbone (Blackwell cu128 build) ---
torch==2.11.0+cu128

# --- Scientific / ML stack ---
numpy==2.4.4
scipy==1.18.0
scikit-learn==1.9.0
pandas==3.0.2
matplotlib==3.10.9
seaborn==0.13.2

# --- MOMENT encoder: HuggingFace stack (momentfm runtime deps) ---
transformers==5.12.1
huggingface-hub==1.21.0
tokenizers==0.22.2
safetensors==0.8.0
regex==2026.6.28

# momentfm itself is installed SEPARATELY with --no-deps (see header):
#     pip install --no-deps momentfm==0.1.4

# --- Pinned transitive dependencies (exact installed versions) ---
# torch
filelock==3.29.0
fsspec==2026.4.0
Jinja2==3.1.6
MarkupSafe==3.0.3
networkx==3.6.1
sympy==1.14.0
mpmath==1.3.0
typing_extensions==4.15.0
setuptools==81.0.0
# huggingface-hub / requests / httpx
hf-xet==1.5.1
requests==2.33.1
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.1
certifi==2026.4.22
idna==3.14
charset-normalizer==3.4.7
urllib3==2.7.0
# typer / rich CLI (pulled by huggingface-hub)
typer==0.25.1
click==8.4.2
shellingham==1.5.4
annotated-doc==0.0.4
rich==15.0.0
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0
colorama==0.4.6
# scikit-learn
joblib==1.5.3
threadpoolctl==3.6.0
narwhals==2.22.1
# matplotlib / pandas
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
pillow==12.2.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
tzdata==2026.2
# shared
packaging==26.2
tqdm==4.67.3
PyYAML==6.0.3
